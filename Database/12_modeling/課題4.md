課題:データベースにおける「NULL」に関するクイズを3問作成してみてください。


### 1.データベースにおける NULL の意味として正しいものはどれ？

A. 空文字（""）
B. 0
C. 未知の値（まだわからない・欠けている）
D. エラー値


### 2. 以下のうち、age カラムが NULL のレコードを正しく抽出するSQL文はどれ？

A. SELECT * FROM users WHERE age = NULL;
B. SELECT * FROM users WHERE age == NULL;
C. SELECT * FROM users WHERE age IS NULL;
D. SELECT * FROM users WHERE age IS NOT TRUE;

### 3.DB設計時にNULLを許容するケースを三つあげよ。

例：ユーザ登録時の任意アドレスフォームをNULLにする